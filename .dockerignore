# Build artifacts — generated inside the image
node_modules
.next
out
dist
build

# Local env (real secrets are injected via compose / docker run -e)
.env
.env.local
.env.*.local

# Dev tooling
.git
.github
.vscode
.idea
.turbo
.vercel

# OS / editor noise
.DS_Store
Thumbs.db

# Logs + test artifacts
*.log
coverage
playwright-report
test-results

# Docker meta — never ship the Dockerfile inside its own image
Dockerfile
docker-compose*.yml
.dockerignore
README.md
docs

# Build artifacts + local DB dumps that shouldn't enter the image
tsconfig.tsbuildinfo
*.sql
backups
prisma/dev.db*
prisma/seed-data
